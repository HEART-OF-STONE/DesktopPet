# 开发 Todo

更新：2026-09-22。只供开发使用，不属于角色包、用户数据或发布包。
当前公开稳定版 v0.9.1，原 P0 / P1、三项桌面体验优化及签名更新功能已实现；公开检查、下载与验签已通过，真实跨版本安装继续跟进。状态标记：✅ 已完成实现和相应验证；⬜ 待完成。

## v0.9.2 · 已通过用户验收，发布准备

- ✅ 版本号同步为 0.9.2；发布说明包含相对 0.9.1 的命名、积分、美元折合与首次读取变化。
- ✅ 默认只补齐近 7 个本地自然日，按修改时间包含仍活跃的旧会话；时间定位、累计值 / 模型基线与保守回退避免跨日整笔累计。
- ✅ 最新尾部快照优先；近期独立游标、进度和短批次推进，完整历史按需开启 / 暂停，不阻塞近期完成状态，远端限频保持独立。
- ✅ 积分美元折合 / 原始积分可切换；详情同时显示，常显默认关闭，比例可配置并恢复参考值。零、缺失、不限量、负余额与小额有明确显示。
- ✅ 小详情将积分与美元折合合为同一行，加“积分”单位并以 = 连接；换算说明与较旧的附加额度默认折叠，主额度始终保留。原生验证覆盖键盘展开、未知 / 不限量 / 负余额和 240px 窄面板长数字。
- ✅ 91 项前端测试、构建和全部 52 项 Rust 测试通过；原生首启 / 重启覆盖美元单位、改比例、常显开关、双布局、历史开关、额度限频与失败保留。
- ✅ 隔离数据目录的本机只读性能测试：约 1.5 秒取得积分快照，约 19 秒完成近期补读；未在源码保存日志原文或账户余额。
- ✅ 用户验收 v0.9.2 预览版，授权检查后提交并上传。
- ⬜ 生成正式签名安装包、提交与上传 GitHub，验证公开附件与自动更新清单。

## Codex 积分余额 · v0.9.2

- ✅ 读取实时接口与本地日志的 credits 合计；兼容字符串 / 数值、零、未知、不限量与新旧字段；保留原值，参考美元折合单独显示，不混用额度重置次数。
- ✅ 桌宠数据框点击展开的小详情直接显示积分及来源 / 时间，后台看板亦可查看；常显默认关闭，可勾选“常显积分余额”，简洁状态条可单独选择积分指标。
- ✅ 分类别、独立时间戳保存积分；旧日志不覆盖新值，省略字段保留旧快照，显式 null 表示未知；失败保留旧值，复用现有限频机制。
- ✅ 一次性有界补读已有日志（最多 8 个文件，各 1 MiB），不清采集游标、不重复累计 Token、不重放提醒。
- ✅ 89 项前端测试、构建、12 项相关 Rust 测试（额度解析 / 调度 / 升级补读）通过；隔离原生首启 / 重启验证常显默认关闭、勾选持久化、桌宠小详情、侧边 / 底部排版、简洁指标及限频 / 失败保留。截图位于 `output/playwright/quota-auto-details.png` 与 `quota-credits-*.png`。
- ✅ 公开源码检查通过；测试使用虚构余额，真实账户数据和凭据未写入源码或文档。
- ⬜ 随下一版安装包发布；当前 GitHub v0.9.1 附件不包含本次功能。

## 内置角色命名 · 下一版

- ✅ 用户确定默认名为「啾咪」；同步静态 / 动画角色、窗口标题、示例包、生成脚本与公开说明。
- ✅ 保留历史角色 ID，兼容已有昵称、自定义默认名、皮肤和备份。
- ✅ 87 项前端测试、构建、Rust 旧存档 / 名称持久化测试及公开源码检查通过。
- ⬜ 随下一版安装包发布新默认名；当前 v0.9.1 附件保持不变。

## 底部额度排版修复 · v0.9.1

- ✅ 修复固定 38px 百分比列与任意换行样式导致 `100%` 分成两行的问题；数值单行右对齐，列宽至少 44px 并可随内容增长，进度条收缩，卡片整体宽度保持 320px。
- ✅ 原生底部布局首启 / 重启验证：双额度 100%、数据框 80% / 100% / 125%、DPR 1–3 与 360px 窄窗口；单额度、长值、空值、详情及气泡边界回归通过。
- ✅ v0.9.1 签名安装包和便携包已发布至 GitHub 稳定 Release，五个附件 SHA256 匹配；截图见 `output/playwright/bottompanel-100-percent.png`。

## 官方仓库与签名更新 · v0.9.0

- ✅ 默认绑定 `HEART-OF-STONE/DesktopPet`，兼容旧设置；支持仓库网址、恢复官方来源。
- ✅ 默认每 6 小时自动检查、手动至少 1 分钟；请求前持久化节流时间，检查失败保留旧结果，可关闭自动检查。
- ✅ 新版本提示、发布说明、下载进度、官方签名验证与主动安装重启。自定义来源只查看版本，不允许应用内安装。
- ✅ 管理窗口权限限制，重复请求互斥，UI 状态伪造不能触发未验证安装；退出时丢弃未安装的内存包。
- ✅ 签名私钥在仓库外保管；源码检查拦截常见凭据及私有数据文件，CI 检查和草稿发布工作流已编写。
- ✅ 本地模拟服务器验证真实签名下载及篡改拒绝；45 项 Rust 测试、原生首启与重启验证通过。
- ✅ 87 项前端测试、源码拦截自测、构建和分发允许列表验证；本地 NSIS、签名、清单及 ZIP 已生成，安装包经发布工具验签。
- ✅ 源码提交 `164c827` 已推送，GitHub Source checks 通过；首个稳定 Release `v0.9.1` 已发布，包含安装包、签名和 `latest.json`。
- ✅ 以无认证请求读取公开版本和清单；隔离 0.9.0 测试身份从正式 GitHub 下载 0.9.1 并验签成功，停在“安装并重启”前。详见 `dev/updater-release-qa-v0.9.1.md`。
- ⬜ 可选：配置 GitHub `release` 环境 Secrets 及 `SIGNED_RELEASES_ENABLED=true`，启用云端签名构建。本次使用本机签名成品，私钥仍只在本机，不影响用户在线更新。
- ⬜ 完成真实跨版本安装与重启验收；本次没有替换用户正在运行的安装。

发布步骤见 `dev/RELEASING.md`；开发资料不进入用户分发包。

## 桌面体验 · v0.8.0（用户确认高优先级 2026-09-18）

本轮仅实现以下三项。29 项前端、42 项 Rust 检查通过；隔离原生首启与重启验证设置、换边、缩放及真实测试窗口全屏避让。实机多屏和特定游戏的覆盖范围另列待验收。

### 1. 侧边数据框自动换边

- ✅ 设置支持自动 / 固定左侧 / 固定右侧；自动模式按当前显示器的可用空间选择，靠右时放角色左侧，靠左时放右侧。
- ✅ 角色居中锚点不随换边改变；边缘附近加入切换缓冲。固定侧空间不足时仍保护整体可见范围；显示器按角色中心选择。
- ✅ 原生左右边缘、固定 / 自动、切换布局及重启设置验证；单双额度和缩放边界纳入已有布局回归。
- ⬜ 实体鼠标跨不同 DPI 显示器拖动、边缘快速反向及断屏恢复继续验收。

### 2. 数据框独立缩放

- ✅ 设置支持 80%–125% 独立缩放、即时生效、恢复 100%、持久化和随偏好备份；正常空间下不改变角色尺寸。
- ✅ 文字、进度条和间距协调缩放，命中区域使用实际显示尺寸；详情移至独立层，维持正常字号和屏幕边界保护。
- ✅ 原生三样式 80% / 100% / 125% 代表档位边界验证；底座长值 / 空数据 / 单双额度 / DPR 1–3 / 窄窗口回归通过。
- ⬜ 实体 4K 主观清晰度和不同 DPI 屏幕连续切换继续验收。

### 3. 全屏时自动避让

- ✅ 默认开启、可关闭；约 0.5 秒本地检测所在屏幕的前台全屏窗口，临时隐藏角色、数据框和气泡，退出后恢复，不调用模型。
- ✅ 临时避让与手动隐藏分开；后台采集继续，恢复时清除旧气泡，不覆盖手动隐藏偏好。备份恢复也遵循避让设置。
- ✅ 原生测试窗口全屏 / 退出、全屏中禁用避让、手动隐藏优先和重启保存通过；普通最大化与其他屏幕几何判定有单元覆盖。
- ⬜ 实际游戏独占全屏、视频 / 演示、不同 DPI 多屏及锁屏 / 休眠组合继续实机验收。

其它候选功能按用户要求留在后续。多屏稳定性及资源占用检查属于持续验收；可配置动作映射继续维持低优先级。

## 贴边与回正修复 · v0.7.7

- ✅ 放置边界按角色 alpha 命中范围、控制条和常显框计算，排除透明窗口留白与临时弹层，留 6 DIP 边距；启动恢复、布局切换使用新边界。
- ✅ 断屏恢复按可见内容相交判断，允许合法负窗口坐标；按可见内容中心选择显示器，覆盖负坐标和 DPI 单元检查。
- ✅ 松手立即清零方向，拖动动作 220ms 后结束；拖动动作时长与 4.2 秒气泡展示分开。贴边时气泡、详情限制到屏幕工作区。
- ✅ 26 项前端、39 项 Rust 检查通过；三布局原生贴边 / 弹层边界 / 松手约 0.23 秒回正 / 周期恢复不跳位 / 重启验证通过。
- ⬜ 实体鼠标跨不同缩放屏幕拖动仍需手动验收；自动化方向事件与几何单元测试不替代该项。

## 底座比例修正 · v0.7.6

- ✅ 按用户反馈将底部卡片由 360px 收窄至 320px，收紧留白，等宽数据列和居中竖分隔线；清除通用按钮 gap 对网格造成的额外间距。
- ✅ 底部美元数值移除“≈”，保留“今日预估”标签、完整金额提示与详情计价说明。
- ✅ 26 项前端检查通过；隔离原生首启 / 重启验证两列文字对齐、卡片宽度、单双额度、空值 / 长数字、窄视口、DPR 1 / 2 / 3、气泡及详情边界。

## 数据框形态切换 · v0.7.5

- ✅ 按用户提供的双行底座图新增底部样式：状态标题、金额 / Token 并排、周 / 5h 独立横条，缺失窗口自动收起。
- ✅ 三种样式：原右侧卡片、底部双行底座、原简洁状态条；偏好设置新增即时切换，Agent 看板原入口同步，选择持久化。
- ✅ 保留既有角色素材，底部控制条悬停显示；收紧内置角色与底座距离，按可用高度约束角色大小，避免放大后气泡越界。
- ✅ 大金额在底部使用中文紧凑格式并保留完整金额悬停提示，空额度排版修正。
- ✅ 前端 26 项、Rust 旧设置兼容检查、原生单双额度 / 空值 / 长数值 / 100%–300% DPR / 135% 角色 / 窄视口 / 气泡与详情 / 三模式切换 / 重启保存通过。
- ⬜ 新增单行全指标横条暂留候选；先维持三种用途明确的样式。

## 自动额度查询 · v0.7.4

- ✅ 主额度缺失、超过 10 分钟或到期后自动查询；新鲜主额度日志延后请求，Spark 不推迟旧主额度查询。
- ✅ 自动 / 手动请求共用至少 10 分钟间隔，失败按 10 / 20 / 40 / 60 分钟退避；重启保留请求预算，暂停 Codex 采集则停止。
- ✅ 查询前持久化预留冷却，成功后更新真实额度，失败保留旧数值；详情和看板显示下次检查、上次尝试及错误。
- ✅ 26 项前端、24 项 Rust 联动测试通过；本地模拟服务验证启动自动更新、并发手动查询去重、重启冷却、失败保留及禁用采集。额外一次只读真实额度查询成功。

## 桌宠交互优化 · v0.7.3

- ✅ 底部控制条默认透明，悬停或聚焦显示；完整控制条参与原生命中区域，名字位置也可唤出。
- ✅ 气泡跟随角色可见区域头顶，支持持久化水平 / 垂直偏移、预览和恢复默认；自动限制窗口边界。
- ✅ 拖动时按水平方向倾斜，反向即时切换，松手恢复沿用原动作时长；过滤细微抖动，纯垂直拖动保持直立。
- ✅ “待更新”补充具体原因、最后额度时间和日志读取说明；不增加请求频率。
- ✅ 长气泡可悬停查看完整文字，气泡整体参与命中，避免其文字区域点击穿透到桌面。
- ✅ 前端 26 项、Rust 32 项、隔离原生首启与重启检查通过；检查记录见 `dev/interaction-qa-v0.7.3.md`。
- ⬜ 实体 4K / 多屏鼠标拖动、反向倾斜和透明区域穿透继续手动验收，自动化事件测试不替代真实鼠标验证。

## 计价提示调整 · v0.7.2

- ✅ 侧边常显卡片隐藏“部分计价”，保留近似金额；点击展开详情后显示部分计价说明。
- ✅ 隔离原生检查及重启验证通过：常显不出现该提示，详情可见，未计价和过期提示继续显示。

## 侧边视觉修正 · v0.7.1

- ✅ 根据用户“没有原本的图好看”的反馈，调大标题和标签至 14px，调整字重、浅色边框、圆角、阴影和卡片宽高比。
- ✅ 侧边样式的角色基准尺寸调整为 288；收回内置角色尾部透明留白，使角色靠近卡片，为自主挪动预留空间。自定义角色不套用内置素材留白规则。
- ⬜ 如用户希望角色本身也接近效果图的柔和光影，另行确认角色素材方向；本次修正排版，没有把生成效果图替换为角色素材。

## 侧边常显数据面板 · v0.7.0

设计选择（2026-09-16）：用户选定侧边布局为主方案，底部横条与双行底座保留为候选；旧版简洁状态条可切换。

- ✅ 侧边常显任务状态、Codex 主额度、今日美元预估与今日 Token。
- ✅ 同时有周 / 5h 窗口时，额度区左右等分：周剩余在左、5h 剩余在右；中间用细竖分隔线，各自显示数值和缩短后的独立进度条。
- ✅ 仅有周窗口时恢复整行进度条；仅有 5h 时按相同原则展示实际窗口，缺失不补成 100%。
- ✅ 保留“预估”、待更新等状态；金额与 Token 区保持整行，详情承载部分计价、Spark、时间和模型明细。
- ✅ 原生隔离测试覆盖单双窗口、100% 与长数字、100%–300% 密度、130% 角色大小、窄视口、详情点击、移回主屏边界和重启；前端 26 项测试与 Rust 旧设置兼容测试通过。
- ⬜ 实体 4K / 多屏鼠标拖动与穿透手动验收，继续沿用下方发布前验收；自动化不代替这一项。

## 额度显示修复 · v0.6.1

- ✅ Codex 主额度优先；按实际返回同时展示 5 小时 / 7 天窗口，兼容 Plus 双窗口与仅有单窗口的账户数据。
- ✅ Spark 友好名称、按类别保存日志快照与更新时间，避免互相覆盖；旧日志不覆盖新值，显式空窗口删除旧值。
- ✅ 桌宠状态条同时显示主额度的两个窗口，详情保留全部类别；过期快照标记待更新，不自动补成 100%。
- ✅ 前端 23 项测试、额度与采集 9 项 Rust 测试、原生四窗口展示及重启持久化验证通过。

## 美元预估 · v0.6.0

- ✅ 今日、近七天及来源 / 模型费用预估；区分无记录、未计价、部分计价和零费用。
- ✅ 可编辑输入、缓存输入和输出单价；按来源与模型精确匹配，修改后重算保留的历史记录。
- ✅ 桌宠详情和常驻状态条显示今日预估；复用现有采集快照，本地计算，不增加模型调用或账单查询。
- ✅ 缓存输入不重复收费、推理 token 不重复累加；异常记录不计价，保留原始用量。
- ✅ 前端 21 项、Rust 27 项及原生隔离数据测试通过，覆盖改价、未知模型、事件去重、权限和重启持久化。

按用户决定，本次仅提供费用预估，不接入真实账单或将订阅额度换算成美元余额。

## 修复 · v0.5.1

- ✅ 高 DPI 清晰度：内置素材每帧 768×768，Canvas 按显示大小与像素密度绘制，保持逻辑尺寸。
- ✅ 同一原生窗口模拟 100% / 150% / 200% / 300% 密度切换、大小调整和回切；修正 WebView2 浮点误差与缩放通知缺失。静态与逐帧均通过，真实跨物理显示器验收仍保留在下方。
- ✅ 更新精灵图、PNG 序列示例与高 DPI 素材制作说明。

## P0 · 已完成

- ✅ 自主陪伴：伸懒腰、张望、打盹、小范围挪动；避免连续重复。
- ✅ 可关闭、三档频率、活动范围；预览与停止入口。
- ✅ 互动、拖动、计时提醒和 Agent 反馈优先；免打扰、隐藏及恢复不补播。
- ✅ 接入助手：日志状态、错误原因、重新检查；区分日志与事件通道。
- ✅ 本机通道真实往返测试；测试不写入任务与用量，不冒充外部 Agent 连接。
- ✅ 事件接入步骤、脚本命令复制、最近真实事件信息。
- ✅ 旧设置兼容、双窗口同步、原生联动与发布包验证。
- ✅ 开发资料移至 dev/，用户说明留在 docs/；发布脚本仅打包允许的文件。

## 发布前验收（持续跟进）

- ⬜ 真实鼠标命中、穿透、拖动和焦点。
- ⬜ 多屏 / DPI / 休眠恢复。
- ⬜ 8 小时稳定性、空闲 CPU、内存及大型日志目录。
- ⬜ 干净 Windows 用户环境、真实 DeepSeek 账户和 Codex 审批 Hook。

## P1 · 已完成

- ✅ 提醒收件箱、已读与未读；免打扰留存、去重、筛选、清理、桌宠未读入口。
- ✅ 角色和偏好完整备份、预览导入、换机恢复入口和持久化撤销点（不混入凭据），原生隔离数据验证通过。
- ✅ 安装包、开机启动、更新提示与正式分发流程实现。
  - ✅ 当前用户 NSIS 安装 / 覆盖更新 / 卸载验证，保留用户数据。
  - ✅ 自启动开关、启动位置更新、静默进入桌宠、卸载清理。
  - ✅ 手动检查 GitHub Releases 实现、版本比较、来源校验与错误提示；不自动下载 / 安装。
  - ✅ 发布清单、校验和脚本与用户指南，开发资料不打包。

### 外部发布条件 · 待验收

- ✅ 已连接本项目正式 GitHub 仓库。
- ✅ 公开稳定版 v0.9.1 发布，远端检查、下载与签名验证通过；安装与重启另行验收。
- ⬜ 可选：提供 Windows Authenticode 代码签名证书，改善未知发布者提示。更新包公钥签名已实现，两者独立。
- ⬜ 真实跨版本升级与干净 Windows 环境验收；本轮安装自动化覆盖同版本 `/UPDATE` 路径，不等于这些项目已完成。

## P2 / 后续

- ⬜ 完全静默更新作为后续候选；当前采用自动检查、主动下载与安装。
- ⬜ AI 对话、可管理记忆、调用预算与删除入口。
- ⬜ Live2D 渲染器、模型与既有任务状态联动。
- ⬜ 可配置动作映射编辑器：按用户决定降为低优先级。优先通过完整角色包提供配套动作，不阻塞 P0 / P1。

已交付：基础陪伴、静态 / 逐帧角色、名称与默认名、Agent 用量看板、常驻状态条、联动演示、自主陪伴、接入助手、提醒收件箱、备份恢复和安装分发。历史规划与验证记录见 dev/docs/，本文件是当前优先级的依据。
