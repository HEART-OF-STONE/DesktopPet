# 桌面体验验收 · v0.8.0

日期：2026-09-19。开发资料，不包含在用户发布包中。

## 实现

- 偏好新增 panelSide（auto / left / right）、panelScale（0.8–1.25）、avoidFullscreen（默认 true），旧数据采用默认值；备份和恢复共用偏好校验。
- 侧边卡片围绕固定角色锚点放置；自动模式有切换缓冲，居中时维持原方向。透明窗口扩展为 1000×720，为双侧和独立放大预留空间，放置边界仍只使用可见内容。
- CSS zoom 缩放常显内容，真实显示区域参与命中；详情通过 portal 移到未缩放的桌宠根层。窄窗口限制侧边容器宽度。
- 原生服务约 512ms 检测前台窗口是否无普通标题栏并覆盖角色所在显示器；排除桌面 / 任务栏及宠物窗口。临时隐藏不写入用户显示偏好，不暂停数据采集。恢复时清除气泡，不夺取输入焦点。

## 已验证

- 29 项前端、42 项 Rust 测试通过；涵盖换边缓冲、固定侧、缩放空间、全屏与最大化区分、其他显示器、手动隐藏优先、旧设置兼容及非法值拒绝。
- native-desktop-experience.js 首启 / 重启通过：固定左右、原生窗口移动触发自动换边、角色局部锚点不变、三样式 80% / 100% / 125%、详情屏幕边界、测试管理窗口原生全屏 / 退出、全屏时关闭避让、手动隐藏不被覆盖、偏好持久化。
- 原生缩放宽度：侧边约 179.2 / 224 / 280，底部 256 / 320 / 400；正常窗口下宠物画布宽度保持一致。
- 底座原生回归首启 / 重启通过：单双额度、长金额、空值、DPR 1 / 2 / 3、窄窗口、135% 角色、详情 / 气泡边界。
- 最终侧边回归首启 / 重启通过：单双额度、长金额、空值、DPR 1 / 1.5 / 2 / 3、130% 角色、窄窗口、控件悬停 / 聚焦、气泡、方向倾斜与边界。修正测试对“30 分钟前”时间文本的瞬时读取竞态，等待界面时钟更新。
- 已查看 output/playwright/desktop-experience-side.png、desktop-experience-bottom.png、desktop-experience-settings.png；控件排版正常，缩放后字形与进度条保持清晰、未溢出。
- 全屏和窗口移动的测试权限仅放在 scripts/fixtures/tauri-test-config.json，正常生产构建不启用；测试数据目录与日常使用隔离。

## 限制

原生测试使用隔离 Tauri 窗口，不能代替不同游戏的独占全屏、实体混合 DPI 跨屏拖动、锁屏 / 休眠、8 小时资源稳定性和主观视觉验收；对应事项继续保留在 Todo。自动更新仅记录后续方案，本版不包含更新器。
