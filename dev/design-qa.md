# 数据框形态 · 设计验收

## v0.7.6 底座收紧（2026-09-17）

用户指出上一版比例与参考有差异、横向偏长。上一轮通过仅证明功能与基本排版，不能视为用户认可视觉效果。

- 发现：360px 宽度使底座显宽；两列额外 margin 将分隔线推向右侧；继承通用按钮 gap:8px，又增加列间及分区间距。
- 修正：宽度 320px（减少约 11%）、圆角 14px、内边距收紧；清除网格 gap，两个指标等宽，标签 / 数值顶端对齐，竖线放在正中。数字为 26px，继续突出金额与 Token。
- 按用户要求去掉底部金额“≈”，保留“今日预估”。计价逻辑及右侧样式不变。
- 同批查看用户原图和最终 bottompanel-dual.png / bottompanel-narrow.png；最终卡片约 320×180，较旧版 360×196 收紧。信息结构沿用参考，实际小尺寸下保留可读字号，并非等比例缩小原图。沿用平涂角色素材。
- 前端 26 项通过；隔离原生首启 / 重启检查通过，覆盖 320px 宽度、两列标签 / 数值对齐、无“≈”、单双额度、空值 / 超长数值、DPR 1 / 2 / 3、窄窗口、放大角色、详情 / 气泡边界及样式保存。
- 范围内功能与溢出检查通过；用户美观验收、实体多屏体验仍不由自动化替代。

---

## v0.7.5 底部双行底座（2026-09-17）

参考：用户附件 `codex-clipboard-3fcbb271-06ea-433a-bc62-94cfaaed5a01.png`。实现截图：`output/playwright/bottompanel-dual.png` 与 `bottompanel-card.png`，相同周 66%、5h 80%、金额 $6.29、Token 340.8 万和工作中状态。原稿含宣传标题，比较范围为宠物与数据卡，不复制宣传文字；以卡片宽度约 792:360 的比例比较布局。程序沿用已有平涂角色，生成稿的额外光影不属于本次素材修改。

### 首轮发现与修正

- P2：极端金额在双列中末位单独换行，降低扫读性。改成底部大金额中文紧凑表示，并通过 title 保留完整值；普通金额仍两位小数。
- 修正空额度的列宽，避免“Codex 主额度”标签挤入进度区域；移除顶部横分隔线不必要的圆角。

### 复核

- 字体 / 字阶：沿用 Segoe UI / Microsoft YaHei；任务标题和指标标签 14px，大数值 25px，额度标签 13px、百分比 16px。完整数据的阅读顺序与原稿一致。
- 间距 / 排布：360px 底座，任务状态一行、金额与 Token 两列、额度每项一行；底座与宠物相接，隐藏控制条不占行高。单额度自然收紧；大额或旧值提示时按实际面板高度限制角色，保持在窗口内。
- 颜色：沿用奶油底、鼠尾草绿、灰绿文字，底部边框略增强；详情复用现有样式。
- 素材：原始高清 PNG / 逐帧渲染保持不变。生成稿角色更柔和的光影属于已知素材差异，不以新素材替换用户角色。
- 内容：匹配示例数据和 Codex 工作中标题；Spark、部分计价及刷新详情继续留在展开区。无假额度或为缺失 5h 留占位行。
- 已在同次查看中打开原图与最新双额度、长金额、空额度截图；并检查单额度、窄窗口截图。长金额不再拆散末位，空值排版无溢出。
- 功能：26 项前端及 Rust 旧设置兼容通过；原生初启与重启均通过三种布局切换、两入口一致性、数值边界、DPR 1 / 2 / 3、135% 角色、窄视口及详情 / 气泡边界。
- 限制：未以自动化替代实体多屏鼠标拖动、完整键盘无障碍或用户主观美观验收。

final result: passed

---

## 历史：侧边数据签

目标：用户确认的 `exec-bd21a9d6-390a-4de0-8ea6-df93b1e52021.png`。示例状态：周 66%、5h 80%、今日预估 $6.29、Token 340.8 万。

## 首轮

原生截图：`output/playwright/sidepanel-dual.png`。与目标图同批打开对照；目标是放大的概念图，比较组件区域的比例、对齐与信息层级，不将宣传标题或壁纸当作应用内容。

- P2：侧边卡片上下留白累计偏多，高度约 285 逻辑像素，显得比宠物重。保持 208 像素宽、12 像素标签、24 像素数字，收紧分区内边距至约 245 像素高。
- 双额度左右对齐、分隔线和两条独立短进度条正确；金额与 Token 全宽呈现，长数值不溢出。
- 字体使用项目既有 Segoe UI / Microsoft YaHei，颜色延续奶油与鼠尾草绿；保留真实角色素材和既有名称 / 设置 / 隐藏控制条，不用概念图替换角色。

## 复核

- 已收紧标题、额度区和两块指标的垂直留白，卡片约 245 逻辑像素高。重新截取同一合成数据状态，与目标图在同批图像输入中对照，数字层级、分隔线、额度并排和全宽指标保持一致，首轮 P2 已解决。
- `sidepanel-single.png` 验证单额度恢复整行；`sidepanel-edge-values.png` 验证 100%、过期标记和超长金额；`sidepanel-300-percent.png` 验证高密度与角色最大测试尺寸。窗口为 640×500 逻辑像素，常规截图 DPR 1，高密度测试至 DPR 3。素材使用原始高清角色，未用生成图替换实际角色。
- 保留实际产品需要的宠物名字 / 设置 / 隐藏按钮；工作中状态沿用已有蓝色状态点。这些是与概念宣传图的明确差异，不影响用户确认的侧边结构。
- 原生自动化复核通过，含点击详情、设置保存、非法样式拒绝、窄视口及重启；无本次范围内未解决的 P0/P1/P2 视觉问题。实体多屏拖动 / 穿透仍在开发 Todo 中单独待验收。

## v0.7.1 纠偏

用户未认可上一版的视觉效果，v0.7.0 的通过结论只足以说明功能与基本排版正常，不能当作已经忠实还原或用户认可。重新检查发现标签 / 标题偏小，角色比例和素材透明边距需要单独处理。

本次将标签和标题调整为 14px，卡片为 224px 宽、约 251px 高；角色基准 288px，内置角色按透明边距收回间隔，额外预留自主挪动空间。柔化边框和阴影，字重与中性文字颜色更接近参考。保留原始角色素材，光影和形体差异已明确说明，不声称像素级还原。

最终复核：重新打开原稿与 `sidepanel-preview.png`，在浅色背景下核对实际组件（背景仅为隔离测试临时预览底，生产窗口仍透明）。标题 / 标签为 14px，卡片 224px 宽、约 251px 高，内置角色视觉间距收紧，周 / 5h 数字、分隔线与独立进度条对齐。原生单双额度、长金额、过期状态、100%–300% DPR、窄视口、详情点击及重启复测通过。该通过结论针对本次明确修正的排版问题，不代表用户已认可美观程度，也不代表原图中角色光影已还原。

final result: passed
