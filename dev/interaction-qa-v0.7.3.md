# 桌宠交互检查 · v0.7.3

日期：2026-09-16。范围：用户提供的四张截图所示控制条、气泡、倾斜动作和旧额度提示。

## 检查步骤与结果

1. 控制条：正常。原来始终可见，现默认透明，悬停原区域显示；移出恢复透明，程序聚焦显示。整个控制条都加入原生命中区域，避免只能对准图标才唤出。截图：[悬停显示](../output/playwright/interaction-controls-visible.png)。窗口本身沿用不抢焦点设置，自动化 focus 检查不代表完成真实键盘无障碍验收。
2. 气泡：正常。原先固定在窗口顶部，现根据角色可见像素区域锚定在头顶附近，默认间隔约 10px；管理面板可调整偏移、预览和恢复默认。截图：[默认位置](../output/playwright/interaction-bubble-default.png)、[调整位置](../output/playwright/interaction-bubble-adjusted.png)、[窄窗口边界](../output/playwright/interaction-bubble-narrow.png)。气泡整体参与命中，文字悬停可查看全文，关闭按钮保留。
3. 倾斜：按用户补充要求调整。原动作是固定右倾，属于短暂动画；现使用原生光标的水平移动方向，左 / 右和反向切换生效，DPI 比例参与抖动阈值，纯垂直拖动为直立。截图：[左倾](../output/playwright/interaction-drag-left.png)、[右倾](../output/playwright/interaction-drag-right.png)。松手沿用 4.2 秒动作后恢复待机。原生事件注入验证了视图，Rust 单测验证方向选择；实际鼠标跨屏拖动仍待手动验收。
4. 额度提示：正常且补充解释。超过 15 分钟、缺少时间、采集暂停或已到重置时间会标记待更新。新增悬停原因和详情说明。10 秒本地日志读取并不保证产生新的额度快照；真实查询仍由完整看板触发。本次没有提高查询频率。

## 验证

- 前端 26 项测试、TypeScript / Vite 构建通过。
- Rust 32 项测试通过；其中凭据写入单测在沙箱内失败，使用随机隔离条目单独提升权限后通过并删除测试条目。与本次变更相关的方向选择、旧设置默认值、边界校验、序列化均通过。
- 原生隔离流程首启和重启均通过，截图只使用合成数据。测试应用关闭，未操作日常用户数据。
- 已打开检查上面的默认气泡、调整气泡、悬停控制条和左右倾斜截图。窄窗口图用于补充自动化边界检查。
- 发布包继续使用明确的六文件允许列表，不含本文件、Todo、源代码、测试数据或用户设置。

## 后续候选

- 气泡偏移按角色记忆：目前是全局偏移，切换高矮不同的角色时可能需要重新微调。
- 侧边面板可切换左 / 右：当前固定右侧，靠近屏幕边缘或使用宽角色时可进一步改善。
- 消息停留时长可调：目前互动气泡与动作共享固定时长，较长文案可以考虑延长停留或悬停暂停。

这些是候选，不在本版实现范围。实体多屏拖动、真实鼠标穿透和完整无障碍体验没有被截图或事件注入测试替代。
