# 首次公开审查

日期：2026-09-19。目标仓库：`HEART-OF-STONE/DesktopPet`。
结论：源码清理、许可整理及经仓库所有者批准的历史脱敏已完成；**审查后的源码具备公开条件**。
源码公开与二进制发布分别验收；未撤销凭据、创建版本标签或发布 Release。

## 范围与方法

- 检查现有全部 3 个提交、分支/标签可达历史、reflog 关联对象，并扩大到本地对象库内所有对象，包括不可达对象；扫描时共 553 个 blob、3 个 commit、0 个 tag。
- 检查工作区已跟踪文件及非忽略的新文件；检查提交身份、提交消息、内容、路径、锁文件与配置。
- 只读扫描 API key / 常见平台 token / JWT / Bearer / 密码赋值 / 私钥 / URL 凭据 / 邮箱 / 绝对路径。命中只记录位置、行号和类型，不输出秘密值；结合源代码语境人工复核。规则扫描不是不存在任意未知格式秘密的绝对证明。
- 检查全部历史 PNG 的文本/EXIF 块；41 个 PNG 对象未发现这些块。仓库唯一历史界面截图经过目视检查。
- 当前 30 个角色/图标与三个历史提交中的各 30 个角色/图标，均用对应版本生成脚本再生成并逐字节比对一致。
- 对比上游文件 Git blob 与当前代码连续五行片段；除许可文本外未发现相同文件，未发现达到阈值的代码片段重合。此检查支持重新实现的来源记录，不声称证明所有概念或短代码均无参考。
- 远端匿名 `ls-remote` 成功且没有任何分支/标签；上传前仍须重新检查，禁止强推、镜像推送或覆盖新出现的远端内容。

## 问题位置与类型

| 位置 | 问题类型 / 状态 |
|---|---|
| 原 Git commits `603e43a`、`b542982`、`1e6794b` 的 author / committer 字段 | 个人姓名/账户标识与个人邮箱；已重写为公开账号及 GitHub noreply 身份 |
| 历史 `scripts/browser-smoke.js`、`scripts/import-smoke.js`、`scripts/rename-smoke.js`、`scripts/native-smoke.js`、`scripts/native-rename-smoke.js` | 开发机绝对工作区路径；当前文件及全部三个公开历史提交均已清理 |
| 当前 `scripts/native-agent-*.js`、`scripts/native-estimate-smoke.js`、`scripts/native-hidpi-smoke.js`、`scripts/native-p0-smoke.js`、`scripts/native-p1-smoke.js` | 开发机绝对工作区路径；已改为相对路径 |
| 本地不可达 Git blob | 含旧版脚本绝对路径；不随普通分支推送，不删除本地恢复对象 |
| `.gitignore` | 凭据、签名材料、运行数据、私人测试产物及截图规则不足；已补充 |
| `.cache/`、`output/`、`.playwright-cli/`、`.tools/`、`src-tauri/target/` | 本机数据、测试运行记录、截图、缓存和产物；保持忽略，不纳入上传 |
| `LICENSE`、`package.json`、`src-tauri/Cargo.toml` | 缺少项目许可证及许可元数据；已补充 MIT |
| `src-tauri/tauri.conf.json`、`scripts/package-release.ps1` | 发布包遗漏许可声明；已加入显式白名单及安装包资源新鲜度检查 |
| `README.md` | 本机产物链接、旧远端说明、发布状态不明确；已改为正式仓库/Releases 地址及未发布说明 |
| `examples/*/pet.json` | 素材许可描述不完整；已嵌入 MIT、版权、许可全文和来源链接 |
| 上游 `assets/**` | 不适用 MIT、无再许可授权；未引入本项目 |
| 开发依赖 `stackback@0.0.2` | 仅包元数据声明 MIT，缺单独上游许可文本；已如实记录，不分发该构建工具 |

`docs/07-agent-guide.md` 的示例安装路径、测试中的虚构路径及 PowerShell 注册表路径不属于个人路径。
脚本 Authorization 命中为运行时变量插值，未存放令牌字面值。第三方原始许可中的作者和公开邮箱是必须保留的归属声明。
`dev/` 开发资料未检出个人邮箱、账户标识、凭据或真实本机绝对路径。
`docs/images/rename-dialog.png` 仅为演示界面，未发现账户信息或私人桌面内容；保留为明确允许的文档截图。

## 凭据结论

在拟上传文件及扫描到的全部 Git 对象中，未发现需撤销或轮换的有效 API Key、Token、密码或签名私钥。
个人邮箱与路径属于隐私泄露风险，不等同于访问凭据泄露。当前没有凭据轮换事项。
`.gitignore` 不能移除历史隐私；禁止将当前清理提交直接接在原历史后推送并宣称历史已清理。

## 许可与素材

- 项目自行实现的源代码、文档、生成脚本及其演示素材按 MIT；第三方保留原许可。
- 上游检查固定于 `83fc01a5aa9d002ea70f5d30d102bd9f65f1ec42`，其代码 MIT、`assets/**` 不在 MIT 范围内。没有使用该项目 npm 依赖，没有复制其鲸鱼、动图、音乐或音效；本项目声音由 `src/core/audio.ts` 合成。
- 保留 MeteorNOX 的原始 MIT 声明、README 致谢及素材边界。来源：https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget/blob/83fc01a5aa9d002ea70f5d30d102bd9f65f1ec42/PROVENANCE.md 。
- 依赖清单覆盖 135 个 npm 锁定包及 322 个 Windows Cargo 依赖（含构建工具）。原始许可、归属及精确版本源码地址汇总于 `THIRD_PARTY_LICENSES.txt`；非当前平台可选包只登记。
- 未发现阻止本项目独立代码采用 MIT 的依赖许可冲突。MPL 组件保持原许可及源码获取途径；Sharp/libvips 的 LGPL 构建工具不进入安装包。不得将所有依赖改标 MIT。
- 未引入归属不明的角色、字体或音效。运行时使用系统字体，不打包字体文件。

## 拟上传与分发边界

上传经审查的源码、构建配置与锁文件、生成脚本、原创演示图片/角色清单、用户文档、许可声明、开发 Todo/设计与验证文档、测试源码及人工合成的最小夹具。
真实账户测试数据、凭据、用户目录、会话日志、临时截图、工具链、依赖目录、构建产物和安装包全部排除。

安装包/便携包白名单为主程序、两个接入脚本、三份用户指南、四份许可文件（安装包另有卸载器）。
开发 Todo 和测试源码不在前端公共资源或 Tauri 资源中；角色导入边界拒绝开发文档，备份只序列化角色和偏好。
示例角色清单只附许可元数据，不附开发记录。保留三个高优先级功能及其最新验收任务，未删除或改写功能实现。

## 验证

- 前端：29 项测试通过；TypeScript 与 Vite 生产构建通过。
- Rust：41 项在沙箱内通过；凭据管理器测试受沙箱写入限制，单项提权后通过（随机隔离测试目标、固定假凭据，结束即清理）。42 项均获验证。
- 发布资源检查：9 个资源严格匹配白名单、来源文件存在；PowerShell 打包/安装测试脚本语法通过。
- 11 个敏感路径/产物忽略探针通过；`git ls-files -ci --exclude-standard` 为空，无已被忽略但仍跟踪的文件。
- 未构建或发布新的安装包；修改许可资源后旧安装包不得直接发布。真实安装分发与实机验收仍是后续 Release 条件。

## 历史脱敏与上传边界

仓库所有者于 2026-09-19 明确同意历史脱敏后上传。先在忽略目录中备份完整本地 Git 数据和全部 237 个审查文件，并校验文件哈希；再在独立临时仓库重建三个历史提交。
身份统一使用公开账号 `HEART-OF-STONE` 及 GitHub noreply 邮箱，脚本工作区路径替换为相对路径。保留原提交顺序、消息、日期及其他文件内容，三个提交哈希相应改变。
当前已审查的 v0.8.0 开发源码、许可和文档作为后续提交；三个高优先级功能和剩余实机验收任务继续保留。

仅将这个独立仓库的 `main` 分支作为首次公开来源；它不包含旧对象、原个人身份、私有恢复备份、工具自动生成的本地检查点或额外分支。
上传前对独立仓库全部对象和最终文件复扫，并核对远端仍为空。普通分支推送不带强制选项、不推送标签或镜像引用；若远端出现其他内容则停止覆盖行为。
本地旧备份仅供私有恢复，不应分享；工作仓库的本地恢复对象也不随此次显式 `main` 推送公开。后续维护禁止未经审查的镜像推送。
